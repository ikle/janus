help: Номер порта сервера авторизации IBF
type: u32
default: 8081
val_help: u32:1-65535; Номер порта

syntax:expression: $VAR(@) >= 1 && $VAR(@) <= 65535 ; "Номер порта доблжен быть в диапазоне от 1 до 65535"
